; Per-directory PHP settings (used by PHP-FPM/CGI when user_ini is enabled).
; Client edit submits many locations[] entries; default max_input_vars (1000) drops fields after the limit.
max_input_vars = 10000
